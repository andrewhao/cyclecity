import Quick
import Nimble
import Cyclecity

class NewRidePresenterSpec: QuickSpec {
  override func spec() {
    describe("#startRecordingGpsTrack") {
      it("tells the interactor to start.") {
      }
    }
  }
}
